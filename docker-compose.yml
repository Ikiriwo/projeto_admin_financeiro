version: '3.14'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - .:/app
      - ./uploads:/app/uploads
      - ./templates:/app/templates
      - ./static:/app/static
    depends_on:
      - db
    env_file:
      - .env
    restart: always
